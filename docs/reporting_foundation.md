# Reporting Foundation

Reports are derived, append-only snapshots that reference existing business records.

A report snapshot records its type, scope, source set, verified generator, and generation-time reference. It does not rewrite source records, send communications, move money, change stock, or become a new source of truth.

Any later export or delivery capability must use a separate contract, approval path, and receipt.