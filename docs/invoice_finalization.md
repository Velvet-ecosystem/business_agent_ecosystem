# Invoice Finalization

Invoice finalization converts an existing local invoice draft into a durable approved record.

The ceremony requires a verified principal, strong-human approval, a completed job, and an invoice draft bound to that same job. The executor re-reads current state before writing the finalization record and receipt.

This slice records approval only. It performs no external action.
