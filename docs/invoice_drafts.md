# Invoice Drafts

Invoice drafting creates a local reviewable record only after a job is completed and matching completion evidence exists.

The slice contains a deterministic record, locked storage, proposal agent, strong-human gate, executor, receipt, and vertical tests.

The intent contract is exact and rejects additional fields.

The output remains a local draft. This slice performs no external action.
